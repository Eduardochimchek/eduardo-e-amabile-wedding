# Public site URL (used for absolute Open Graph URLs when set in wedding config too)
# NEXT_PUBLIC_SITE_URL=https://seu-dominio.vercel.app

# Optional webhook that receives RSVP JSON payloads from POST /api/rsvp
# Never commit real secrets. Configure this in Vercel Environment Variables.
RSVP_WEBHOOK_URL=
